export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  categoryId: string;    // Nome correto conforme seu código da página
  stockQuantity: number; // Nome correto conforme seu código da página
  createdAt: string;
}